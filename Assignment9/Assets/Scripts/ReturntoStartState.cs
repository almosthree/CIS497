using A9;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReturntoStartState : MonoBehaviour
{
    private CombatImplementation combatImplementation;

    public ReturntoStartState(CombatImplementation combatImplementation)
    {
        this.combatImplementation = combatImplementation;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
